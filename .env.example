# Used by docker-compose.yml. Copy this file to .env (same folder as docker-compose.yml)
# and fill in your key, or just skip it — the app works without one, model search will
# just return no results until you add a key.

# Free key from https://api-ninjas.com/api/motorcycles
API_NINJAS_KEY=
