services:
  clank:
    build: .
    image: clank:latest
    ports:
      - "4000:4000"
    environment:
      - API_NINJAS_KEY=${API_NINJAS_KEY:-}
    volumes:
      - clank-data:/data
    restart: unless-stopped

volumes:
  clank-data:
